# UI review 2 (same temporary chat, 19/09/2026 ~2:50 pm Sydney)

Inputs: the rebuilt /matrix with drawer and the phone home after all eight pivot-1 changes. Verbatim verdict below.

---

VERDICT: PASS

The desktop matrix now reads as a calibrated scientific instrument rather than a dashboard. The SHOW controls have the correct ledger vocabulary and no longer compete with the matrix.

The drawer hierarchy is resolved. Direct relation → bridges → checks → search record → evidence → coordinates scans naturally, and the wrapped bridge chain preserves the actual scientific path instead of turning it into UI truncation.

The seven-check strip now works at this density. TYPE / ENERGY / FREE / BOUND / DIM / BC / MAG is immediately parseable, the glyphs dominate appropriately, and there is no colour-semantic clutter.

Matrix marks survive the density well. Established, demonstrated, candidate and theoretical states are perceptually distinct; the darker 1px not-searched corner is now visible without becoming a competing symbol. Addresses remain subordinate until active.

The desktop composition has a strong hierarchy: title/context → filters → probe → matrix → evidence drawer. The matrix remains the primary object even with the drawer open.

The phone treatment is now convincingly the same instrument rather than a mobile redesign. The 112px-ish row rail and 32×28-ish cells preserve the matrix, while horizontal continuation handles information density rather than collapsing it into cards.

The two-row phone ledger is substantially better. It provides immediate dataset scale without pushing the probe and matrix below an oversized statistics block; overflow reads as intentional continuation.

No banned-default regression is visible: no cards, pills, gradients, shadows, glow, generic dashboard containers, or ornamental responsive substitutions. The build now matches the Calibrated Ledger system closely enough that further visual changes should come from real-use findings rather than speculative polishing.