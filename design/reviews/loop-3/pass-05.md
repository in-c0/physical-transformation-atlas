# Pass 5 — the drawer as a scientific question (20/09/2026 ~10:50 am Sydney)

Focus sent: read three cells (D.01×C.23 no bridge; D.11×C.17 bridges; D.23×C.01 no bridge) and judge whether
the drawer poses a well-formed question, what a physicist cannot yet do from it, one-line "what would
change this cell" copy per status, redundancy. ChatGPT (2m 29s). Verdict: PIVOT.

## Findings (condensed)

- State the question explicitly under the subtitle: "Does the atlas record a direct {coupling} relation
  driven by {driver}?" — the matrix semantics are narrower than the title suggests.
- Zero bridges ≠ physics rejects it: say "Physics screen · Not assessed — no recorded composition to run
  the checks on."
- Add the status-transition contract: one generated "What would change this cell" sentence per status
  (ten strings supplied; candidate's positive transition is a "direct relation", not "demonstration").
- Regression: matrix candidate status ignored `structural_kind`; an atomic or backtracking bridge could promote
  a cell. Require a `composition` bridge.
- Bridges were ordered by length; reuse the research comparator so the drawer's default bridge is the best
  composition.
- Compress the three constituent rows into one; show the four core checks in the drawer with the full strip on
  the path page; keep coordinates, provenance, evidence split, overlap.
- Missing actions: Show in graph; Prepare search (a reviewable query bundle from aliases, never auto-recorded);
  Copy question (Markdown + JSON snapshot with dataset revision); later "Propose relation" into a review queue.
- Materials & regimes disclosure per bridge from `observed_in`: "shared material recorded / no shared material
  recorded in the atlas / insufficient material records" — never "no compatible material exists".

## Applied

See the pass-5 commit.
